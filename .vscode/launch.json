{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: start",
      "request": "launch",
      "command": "npx expo start --tunnel",
      "cwd": "${workspaceFolder}"
    }
  ]
}
